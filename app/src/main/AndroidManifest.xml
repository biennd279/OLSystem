<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.teamseven.ols" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_remind"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_remind"
        android:supportsRtl="true"
        android:theme="@style/Theme.OLSystem">
        <activity android:name=".ui.main.ClassActivity"
            android:theme="@style/Theme.OLSystem">

        </activity>
        <activity
            android:name=".ui.login.ForgotPasswordActivity"
            android:theme="@style/ForgotPassword">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
<!--            <meta-data-->
<!--                android:name="android.support.PARENT_ACTIVITY"-->
<!--                android:value=".ui.login.LoginActivity" />-->
        </activity>
        <activity
            android:name=".ui.login.GoToLoginActivity"
            android:theme="@style/Theme.OLSystem.NoActionBar">

        </activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.OLSystem.NoActionBar">

        </activity>
    </application>

</manifest>